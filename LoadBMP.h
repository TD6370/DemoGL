//#pragma once
//class LoadBMP
//{
//};

#ifndef loadBmp_H
#define loadBmp_H

unsigned char* LoadBmp(const char* filePath, unsigned int* width, unsigned int* height);
//static unsigned char* LoadBmp(const char* filePath, unsigned int* width, unsigned int* height);

#endif

